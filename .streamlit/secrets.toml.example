# Copy to .streamlit/secrets.toml for LOCAL streamlit dev.
# In Streamlit Cloud, paste these into "App settings → Secrets" instead.
# Both files are gitignored — never commit real secrets.

CLV_URL = "https://your-clv-service.up.railway.app"
STUDIO_URL = "https://your-pastry-service.up.railway.app/dashboard/studio"
